th_gensokyo_culture_chronicle = {
	monthly_spawn_chance = 1
	hint_tag = hint_th_gensokyo_culture_chronicle

	can_start = {
		always = yes
	}

	can_end = {
		always = no
	}

	visible = {
		is_human = yes
	}

	on_start = {
		every_country = {
			limit = { is_human = yes }
			trigger_event_silently = th_culture_chronicle.1
		}
	}
}
