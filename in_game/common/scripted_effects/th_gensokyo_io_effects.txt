### ===========================================================
### 幻想乡IO — 脚本化效果接口
### 全部数值变更统一走本文件接口（对齐设计文档 §2.1/§2.3/§2.5）
### 注：原「恶名」变量已迁移为「价值观」社会价值观
###     societal_value:th_gensokyo_amity_vs_belligerence（-100亲善 ~ +100好战）
### ===========================================================

### 幻想乡团结度 增加（在IO scope上调用，设计§2.1）
th_gensokyo_io_unity_var_add_effect = {
    change_variable = {
        name = th_gensokyo_unity_var
        add = $value$
    }
    clamp_variable = {
        name = th_gensokyo_unity_var
        min = 0
        max = 100
    }
}

### 幻想乡团结度 减少（在IO scope上调用，设计§2.1 损失表）
th_gensokyo_io_unity_var_subtract_effect = {
    change_variable = {
        name = th_gensokyo_unity_var
        subtract = $value$
    }
    clamp_variable = {
        name = th_gensokyo_unity_var
        min = 0
        max = 100
    }
}

### 幻想乡个人贡献度 增加（在国家scope上调用，设计§2.3）
### 2026-08-21（q5 确认 + 需求 3）：删除按价值观档位打折段
### （好战≥40 → ×0.3 等三档 —— 阶段性修正系统，与 auto_modifier/
### 弃民系数同批删除；价值观后果由价值观条渐进式修正承担）
th_gensokyo_io_contribution_var_add_effect = {
    change_variable = {
        name = th_gensokyo_personal_contribution_var
        add = $value$
    }
}

### 幻想乡个人贡献度 直接结算（无价值观惩罚，供内部换算用；国家scope）
th_gensokyo_io_contribution_var_raw_add_effect = {
    change_variable = {
        name = th_gensokyo_personal_contribution_var
        add = $value$
    }
}

### 幻想乡个人影响力 增加（在国家scope上调用，设计§2.4 投资1:1）
th_gensokyo_io_influence_var_add_effect = {
    change_variable = {
        name = th_gensokyo_personal_influence_var
        add = $value$
    }
}

### 幻想乡个人影响力 减少（在国家scope上调用）
th_gensokyo_io_influence_var_subtract_effect = {
    change_variable = {
        name = th_gensokyo_personal_influence_var
        subtract = $value$
    }
}

### 价值观 趋向好战（在国家scope上调用，社会价值观尺度，正值=向右/好战）
### 原 th_gensokyo_io_notorious_reputation_var_add_effect（恶名增加，0-100）
### 现直接操作 societal_value，引擎自动钳制 -100 ~ +100
th_gensokyo_values_shift_belligerent_effect = {
    change_societal_value = {
        type = th_gensokyo_amity_vs_belligerence
        value = $value$
    }
}
